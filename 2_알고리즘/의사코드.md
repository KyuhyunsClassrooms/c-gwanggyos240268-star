# 2. 알고리즘 (의사 코드) (10점)

## 1. `main` 함수의 전체 흐름
(프로젝트의 전체 작동 로직을 의사 코드로 작성하세요.)
시작
    반복 (사용자가 종료를 선택할 때까지)
        메뉴 출력 (1. 등록, 2. 검색, 3. 목록, 4. 종료)
        메뉴 번호 입력 받기

        만약 입력값이 1이면:
            도서 등록 함수 호출
        아니면 입력값이 2이면:
            도서 검색 함수 호출
        아니면 입력값이 3이면:
            도서 목록 출력 함수 호출
        아니면 입력값이 4이면:
            "프로그램 종료" 출력 후 종료
        아니면:
            "잘못된 입력입니다" 출력
    반복 끝
종료
```

## 2. 핵심 기능 1: (예: 학생 등록 함수)
```
(핵심 기능 1의 논리적 절차를 작성하세요.)

시작
    만약 현재 등록된 도서 수가 최대치(MAX_BOOKS)를 초과하면:
        "더 이상 등록할 수 없습니다" 출력
        종료
    아니면:
        도서 제목 입력 받기
        저자 입력 받기
        출판년도 입력 받기
        입력받은 정보 저장 (library[count]에)
        등록된 도서 수 count 증가
        "도서가 등록되었습니다" 출력
끝

```

## 3. 핵심 기능 2: (예: 학생 검색 함수)
```
시작
    검색할 제목 입력 받기
    found = false
    전체 도서 목록을 순회하면서
        만약 입력한 제목이 저장된 도서 제목에 포함되어 있으면:
            해당 도서의 제목, 저자, 출판년도 출력
            found = true
    반복 끝
    만약 found가 false이면:
        "검색 결과가 없습니다" 출력
끝

```
